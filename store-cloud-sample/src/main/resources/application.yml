app:
  id: store-sample

apollo:
  meta: http://127.0.0.1:8080

spring:
  application:
    name: store-sample
  config:
    import:
      - optional:apollo://application
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        fail-fast: false

server:
  port: 19080
